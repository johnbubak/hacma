{
  "name": "HACMA - HA-Add-on Fabrikator",
  "version": "1.0.0",
  "slug": "hacma",
  "description": "Erzeugt dynamisch neue HA-Add-ons basierend auf einer Docker Compose YAML-Definition.",
  "url": "https://github.com/johnbubak/hacma", 
  "startup": "once",
  "boot": "manual",
  "arch": ["amd64", "aarch64"],
  "privileged": true,
  "host_network": true,
  "map": ["config:rw"],
  "options": {
    "project_name_selector": "none",
    "compose_yaml": "",
    "action": "none"
  },
  "schema": {
    "project_name_selector": {
      "name": "1. Project Selector", 
      "description": "Select an existing project to edit. Action 'load_config' required before saving.", 
      "type": "select",
      "options": ["none"],
      "default": "none"
    },
    "compose_yaml": {
      "name": "2. Compose YAML Definition", 
      "description": "Insert the Docker Compose YAML here.", 
      "type": "textarea"
    },
    "action": {
      "name": "3. Action", 
      "description": "The action is executed upon saving (the add-on starts once).",
      "type": "select",
      "options": ["none", "load_config", "generate_and_up", "down_and_remove"],
      "default": "none"
    }
  }
}